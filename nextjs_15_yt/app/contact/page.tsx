"use client";

export default function Contact(){
    const key = 'very secret key'; // this will be exposed to browser (go to DevTool -> Debugger to checkout)
    console.log('this is contact')
    return (
        <h1>Contact</h1>
    )
}